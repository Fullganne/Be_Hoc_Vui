// Has grade-selector
// index, account, toan_1

// git commit -m"modify whole styling structure and change about.html to introduce to prevent misunderstanding"

const randomRange = (min, max) => Math.floor(Math.random()*(max-min)) + min;

function swap(a, b) {
    let temp = a;
    a = b;
    b = temp;
    return;
}